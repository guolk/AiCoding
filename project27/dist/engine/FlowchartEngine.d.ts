import { INode, Point, IEngineConfig, IViewState, NodeType, AlignType, DistributeType } from '../types/index.js';
import { BaseNode } from '../nodes/index.js';
import { BaseConnection } from '../connections/index.js';
import { ImportResult } from '../export/index.js';
export declare class FlowchartEngine {
    private canvas;
    private ctx;
    private config;
    private nodes;
    private connections;
    private viewState;
    private mouseState;
    private selectionState;
    private historyManager;
    private alignmentManager;
    private miniMap;
    private exportManager;
    private importManager;
    private animationFrameId;
    private isDirty;
    constructor(canvas: HTMLCanvasElement, config?: Partial<IEngineConfig>);
    private setupEventListeners;
    private handleMouseDown;
    private handleMouseMove;
    private handleMouseUp;
    private handleMouseLeave;
    private handleWheel;
    private handleKeyDown;
    private getCanvasPoint;
    private canvasToWorld;
    private worldToCanvas;
    private getNodeAtPoint;
    private getPortAtPoint;
    private getConnectionAtPoint;
    private getNodeById;
    private getPortById;
    private getPortWorldPosition;
    private updateConnections;
    private saveState;
    private undo;
    private redo;
    private deleteSelected;
    private selectAll;
    private clearSelection;
    private markDirty;
    private startRenderLoop;
    private render;
    private drawGrid;
    private drawNodes;
    private drawSelectionBorder;
    private drawNodePorts;
    private drawConnections;
    private drawConnectionPreview;
    addNode(type: NodeType, position: Point, title: string, config?: Partial<INode>): BaseNode;
    removeNode(nodeId: string): boolean;
    alignSelected(alignType: AlignType): void;
    distributeSelected(distributeType: DistributeType): void;
    exportToJSON(): string;
    exportToSVG(options?: {
        width?: number;
        height?: number;
        padding?: number;
        backgroundColor?: string;
    }): string;
    importFromJSON(jsonString: string): ImportResult;
    canUndo(): boolean;
    canRedo(): boolean;
    getNodes(): BaseNode[];
    getConnections(): BaseConnection[];
    getSelectedNodes(): string[];
    getSelectedConnections(): string[];
    setViewState(offset: Point, scale: number): void;
    getViewState(): IViewState;
    setConfig(config: Partial<IEngineConfig>): void;
    destroy(): void;
}
